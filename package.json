{
  "name": "react-chrome-ext",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && nodemon -r dotenv/config ./index.ts",
    "ext": "cd extension && npm run dev",
    "py": "python -m flask --app ./py-server/app.py run --port=3000 --debug"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "extension",
    "backend",
    "landing-page"
  ],
  "devDependencies": {
    "@types/node": "^18",
    "typescript": "^5.2.2"
  }
}
